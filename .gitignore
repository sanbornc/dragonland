# Full-resolution source images — keep compressed copies in assets/ instead
IMG_*.png

# OS cruft
.DS_Store

# Personal, untracked notes (e.g. combat optimization cheat-sheets) — never committed
*.local.md
